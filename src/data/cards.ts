import type { CardTemplate } from '@/lib/_model';

export const cards: Record<string, CardTemplate> = {
  lion: {
    id: 'lion',
    name: 'Savannah Lion',
    cost: 1,
    power: 2,
    health: 1,
  },
};
